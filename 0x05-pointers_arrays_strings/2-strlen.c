#include "main.h"
/**
 * _strlen - returns the length of a string
 * @s: a pointer to an int that returns length
 * Return: Always 0;
 */
int _strlen(char *s)
{
int l;

for (l = 0; s[l] != '\0'; l++)

{
continue;
}
return (l);
  
