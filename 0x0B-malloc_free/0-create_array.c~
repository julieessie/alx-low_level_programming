#include "main.h"
/**
 * create_array - creates and array
 * @size: size of an array
 * @c: char
 * Return: nothing
 */
char *create_array(unsigned int size, char c)
{
char *p;
unsigned int space;

 if (size == 0)
   {
     return
  
